# 🧪 Local Testing Guide

**브랜치**: `feature/frontend-ui-improvements`
**서버 시작됨**: ✅ http://localhost:8000
**테스트 날짜**: 2026-01-21

---

## 🚀 로컬 서버 실행 중

```bash
✅ HTTP Server: http://localhost:8000
✅ PID: 94022
✅ Status: Running
```

---

## 🌐 테스트 URL

### 메인 페이지
```
http://localhost:8000/static/index.html
```

브라우저에서 위 URL을 열어주세요!

---

## ✅ 테스트 체크리스트

### 1. Scene 여백 최소화 테스트

**목표**: Scene 카드 간격이 줄어들었는지 확인

#### 테스트 절차:
1. [ ] 페이지 로드 후 Scene 카드 확인
2. [ ] 이전보다 간격이 줄어들었는지 육안 확인
3. [ ] 한 화면에 더 많은 Scene이 보이는지 확인

**예상 결과**:
- ✅ Scene 카드 padding: 15px (이전 20px)
- ✅ 카드 간 margin: 10px (이전 15px)
- ✅ Textarea 높이: 60px (이전 80px)

---

### 2. 자동 저장 + 수동 저장 테스트

**목표**: 입력 시 자동 저장 및 복원 확인

#### 테스트 절차:
1. [ ] Scene 1의 나레이션에 "테스트 입력1" 입력
2. [ ] "✓ 자동 저장됨" 메시지가 2초간 표시되는지 확인
3. [ ] 브라우저 F5로 새로고침
4. [ ] "✓ 이전 작업을 불러왔습니다" 메시지 확인
5. [ ] 입력한 "테스트 입력1"이 그대로 있는지 확인
6. [ ] 💾 수동 저장 버튼 클릭
7. [ ] "✓ 저장되었습니다" 성공 메시지 확인

**예상 결과**:
- ✅ 입력과 동시에 자동 저장
- ✅ 새로고침 후 데이터 복원
- ✅ 수동 저장 버튼 정상 동작

**개발자 도구 확인**:
```javascript
// Console에서 확인
localStorage.getItem('aiVideo_scenes')
// → Scene 데이터 JSON 확인
```

---

### 3. Global 이미지 프롬프트 테스트

**목표**: 전체 프롬프트가 모든 Scene에 적용되는지 확인

#### 테스트 절차:
1. [ ] 페이지 상단 청록색 박스 확인
2. [ ] Global prompt에 "pencil sketch, vintage paper" 입력
3. [ ] "✓ 자동 저장됨" 확인
4. [ ] Scene 1에 "A wise philosopher" 입력
5. [ ] Scene 2 추가 후 "A peaceful garden" 입력
6. [ ] F5 새로고침
7. [ ] Global prompt와 Scene 프롬프트가 모두 복원되는지 확인

**예상 결과**:
- ✅ 청록색 박스로 Global prompt 구분
- ✅ 자동 저장됨
- ✅ 영상 생성 시 각 Scene에 Global prompt 자동 추가
  - Scene 1: "A wise philosopher, pencil sketch, vintage paper"
  - Scene 2: "A peaceful garden, pencil sketch, vintage paper"

**개발자 도구 확인**:
```javascript
localStorage.getItem('aiVideo_globalPrompt')
// → "pencil sketch, vintage paper"
```

---

### 4. Drag & Drop Scene 순서 변경 테스트

**목표**: 마우스/터치로 Scene 순서 변경 확인

#### 테스트 절차:
1. [ ] Scene 2개 이상 생성
2. [ ] Scene 1의 ☰ 핸들 확인
3. [ ] ☰ 핸들을 마우스로 클릭 (또는 터치)
4. [ ] Scene 2의 위치로 드래그
5. [ ] 드래그 중 시각적 피드백 확인:
   - 드래그 중인 카드: 반투명 (opacity: 0.5)
   - 드롭 대상: 점선 테두리 (dashed border)
6. [ ] 드롭 후 "✓ Scene 순서가 변경되었습니다" 메시지
7. [ ] Scene 번호가 자동으로 업데이트되는지 확인
8. [ ] F5 새로고침 후 순서가 유지되는지 확인

**예상 결과**:
- ✅ ☰ 핸들 표시
- ✅ 드래그 가능 (cursor: grab → grabbing)
- ✅ 시각적 피드백 (opacity, border)
- ✅ 순서 변경 성공 메시지
- ✅ 자동 저장 및 복원

**모바일 테스트** (선택):
- [ ] iOS Safari에서 터치 드래그
- [ ] Android Chrome에서 터치 드래그

---

### 5. 프롬프트 히스토리 테스트

**목표**: 영상별 프롬프트 저장 및 확인

#### 사전 준비:
⚠️ **이 테스트는 실제 API 서버가 필요합니다**
- API URL: https://aivideo.brut.bot (기본값)
- 또는 로컬 FastAPI 서버 실행

#### 테스트 절차 (API 서버 있을 때):
1. [ ] Global prompt 입력: "minimalist style"
2. [ ] Scene 1: "A philosopher"
3. [ ] Scene 2: "A mountain"
4. [ ] 🎬 영상 생성 버튼 클릭
5. [ ] 작업 완료 대기
6. [ ] 영상 목록에서 "📝 프롬프트" 버튼 클릭
7. [ ] 모달 팝업 확인
8. [ ] 모달 내용 확인:
   - [ ] 생성 시간
   - [ ] Global prompt
   - [ ] Scene별 나레이션
   - [ ] Scene별 이미지 프롬프트
   - [ ] 최종 합성 프롬프트
9. [ ] 모달 외부 클릭으로 닫힘 확인
10. [ ] ✕ 버튼으로 닫힘 확인

**예상 결과**:
- ✅ 모달 팝업 표시
- ✅ 전체 프롬프트 정보 표시
- ✅ Scene별 최종 합성 프롬프트 확인
  - "A philosopher, minimalist style"
  - "A mountain, minimalist style"

#### 테스트 절차 (API 서버 없을 때):
1. [ ] 브라우저 개발자 도구 > Console
2. [ ] 다음 코드 실행:
```javascript
// 테스트 히스토리 저장
localStorage.setItem('aiVideo_promptHistory', JSON.stringify({
  'test_video.mp4': {
    timestamp: new Date().toISOString(),
    globalPrompt: 'minimalist style, pencil sketch',
    scenes: [
      {
        narration: '인생은 고통이다.',
        image_prompt: 'A wise philosopher'
      },
      {
        narration: '그러나 극복할 수 있다.',
        image_prompt: 'A strong warrior'
      }
    ],
    scenesCount: 2
  }
}));

// 영상 목록 HTML 추가 (임시)
document.getElementById('videos-container').innerHTML = `
  <div class="video-item">
    <div class="video-name">test_video.mp4</div>
    <div class="video-meta">크기: 50.00 MB<br>생성: ${new Date().toLocaleString('ko-KR')}</div>
    <div class="button-group">
      <button class="btn btn-success">⬇️ 다운로드</button>
      <button class="btn btn-info" onclick="viewPromptHistory('test_video.mp4')">📝 프롬프트</button>
    </div>
  </div>
`;
```
3. [ ] "📝 프롬프트" 버튼 클릭
4. [ ] 모달에서 데이터 확인

**개발자 도구 확인**:
```javascript
localStorage.getItem('aiVideo_promptHistory')
// → 히스토리 JSON 확인
```

---

### 6. UI/UX 전반 테스트

#### 디자인 일관성:
- [ ] 보라색 그라데이션 유지 (#667eea → #764ba2)
- [ ] Border radius 일관성 (8px, 12px)
- [ ] 버튼 호버 효과 정상 동작

#### 반응형 테스트:
- [ ] 브라우저 창 크기 조절 (1920px → 768px → 375px)
- [ ] 모바일 크기에서 레이아웃 깨지지 않음
- [ ] Scene 카드 모바일에서 세로 배치

#### 접근성:
- [ ] Tab 키로 포커스 이동
- [ ] Enter 키로 버튼 클릭
- [ ] 포커스 인디케이터 표시

---

## 🔍 브라우저 개발자 도구 활용

### Chrome DevTools

#### Console 탭
```javascript
// LocalStorage 데이터 확인
localStorage.getItem('aiVideo_scenes')
localStorage.getItem('aiVideo_globalPrompt')
localStorage.getItem('aiVideo_promptHistory')

// 모든 키 확인
Object.keys(localStorage)

// 데이터 초기화 (테스트 후)
localStorage.clear()
```

#### Application 탭
1. 좌측 메뉴 > Local Storage > http://localhost:8000
2. 저장된 키 확인:
   - `aiVideo_scenes`
   - `aiVideo_globalPrompt`
   - `aiVideo_promptHistory`
3. 값 클릭하여 JSON 데이터 확인

#### Network 탭
- API 호출 없음 (순수 클라이언트 사이드)
- 단, 영상 생성 시 `/api/create-video` 요청 확인

---

## 🐛 알려진 이슈 테스트

### 1. LocalStorage 용량 테스트
```javascript
// Console에서 테스트
let testData = [];
for (let i = 0; i < 1000; i++) {
  testData.push({
    narration: 'Test narration ' + i,
    image_prompt: 'Test prompt ' + i
  });
}
localStorage.setItem('test_large_data', JSON.stringify(testData));
// → Error 발생 시 5MB 한계 확인
```

### 2. Drag & Drop 브라우저 호환성
- [ ] Chrome (최신): 정상 동작 예상
- [ ] Safari (최신): 정상 동작 예상
- [ ] Firefox (최신): 정상 동작 예상
- [ ] Edge (최신): 정상 동작 예상
- [ ] IE11: **지원 안 됨** (예상됨)

---

## 📸 스크린샷 캡처 가이드

### 캡처할 화면:
1. **전체 UI**
   - Scene 설정 전체 화면
   - Global prompt 박스 포함

2. **자동 저장 인디케이터**
   - "✓ 자동 저장됨" 표시 순간

3. **Drag & Drop 중**
   - 드래그 중인 Scene (반투명)
   - 드롭 대상 (점선 테두리)

4. **프롬프트 히스토리 모달**
   - 모달 전체 화면
   - 데이터 표시 확인

---

## 🚨 버그 발견 시

### 버그 리포트 양식:
```markdown
## 버그 제목
간단한 설명

## 재현 방법
1. 첫 번째 단계
2. 두 번째 단계
3. ...

## 예상 동작
무엇이 일어나야 하는가?

## 실제 동작
무엇이 일어났는가?

## 환경
- 브라우저: Chrome 120.x
- OS: macOS 14.x
- 화면 크기: 1920x1080

## 스크린샷
(있으면 첨부)

## 개발자 콘솔 에러
(있으면 첨부)
```

---

## ✅ 테스트 완료 후

### 체크리스트:
- [ ] 모든 기능 정상 동작
- [ ] 버그 없음 또는 문서화됨
- [ ] 크로스 브라우저 테스트 완료
- [ ] 스크린샷 캡처 완료 (선택)

### 다음 단계:
```bash
# 서버 종료
kill 94022

# 또는
lsof -ti:8000 | xargs kill -9

# main 브랜치로 merge 준비
git checkout main
git merge feature/frontend-ui-improvements
git push origin main
```

---

## 📞 문제 발생 시

### 서버 재시작:
```bash
# 기존 서버 종료
lsof -ti:8000 | xargs kill -9

# 새로 시작
cd /Users/jinkane/Desktop/project/aiVideo
python3 -m http.server 8000
```

### 캐시 초기화:
- Chrome: Cmd+Shift+R (macOS) / Ctrl+Shift+R (Windows)
- Safari: Cmd+Option+E
- Firefox: Ctrl+Shift+R

### LocalStorage 초기화:
```javascript
// 개발자 도구 Console에서
localStorage.clear()
location.reload()
```

---

## 🎯 성공 기준

### 모든 기능이 다음 조건을 만족하면 테스트 통과:
1. ✅ Scene 여백 최소화 → 화면에 더 많은 Scene 표시
2. ✅ 자동 저장 → 새로고침 후 데이터 복원
3. ✅ Global prompt → 모든 Scene에 적용
4. ✅ Drag & Drop → 순서 변경 성공
5. ✅ 프롬프트 히스토리 → 모달 정상 표시
6. ✅ 버그 없음
7. ✅ 성능 정상 (로딩 < 200ms)

---

**Happy Testing! 🎉**

테스트 중 궁금한 점이 있으면 언제든지 질문해주세요.
